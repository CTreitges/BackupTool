# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This project is in its initial state — only a PyCharm-generated `main.py` stub exists. The backup tool has not been implemented yet.

## Environment

- Python 3.13 via `.venv` (activate with `.venv\Scripts\activate` on Windows)
- Run the project: `python main.py`
- Install dependencies: `pip install -r requirements.txt` (once created)

## Conventions

- Use the `.venv` virtual environment for all Python execution
- PyCharm is the primary IDE (`.idea/` config is tracked)
